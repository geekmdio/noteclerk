version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: integration
      POSTGRES_PASSWORD: testing
      POSTGRES_DB: noteclerk
    ports:
      - 5433:5432
